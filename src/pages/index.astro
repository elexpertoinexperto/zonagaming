---

---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>Zona Gaming | Componentes PC</title>
    <style>
      :root {
        color-scheme: dark;
        color: #e6f7ff;
        background: #05070b;
        font-family: Inter, system-ui, sans-serif;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        background: radial-gradient(circle at top, rgba(0, 255, 162, 0.12), transparent 28%),
          radial-gradient(circle at bottom right, rgba(0, 184, 255, 0.16), transparent 26%),
          #05070b;
      }

      .page {
        max-width: 1200px;
        margin: 0 auto;
        padding: 32px 24px 64px;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 16px;
        padding: 16px 0;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 12px;
        font-size: 1.1rem;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: #7cffcb;
      }

      .brand span {
        display: inline-flex;
        width: 14px;
        height: 14px;
        border-radius: 999px;
        background: linear-gradient(135deg, #00ffe1, #1bb7ff);
      }

      nav {
        display: flex;
        align-items: center;
        gap: 20px;
        font-size: 0.95rem;
      }

      nav a {
        color: #c8d8e8;
        text-decoration: none;
        transition: color 0.2s ease;
      }

      nav a:hover {
        color: #7cffcb;
      }

      .hero {
        display: grid;
        gap: 32px;
        padding: 80px 0 48px;
      }

      .hero-copy {
        max-width: 620px;
      }

      .hero-copy small {
        color: #7cffcb;
        letter-spacing: 0.18em;
        text-transform: uppercase;
      }

      .hero-copy h1 {
        margin: 18px 0;
        font-size: clamp(2.8rem, 5vw, 4.8rem);
        line-height: 0.95;
      }

      .hero-copy p {
        margin: 20px 0;
        color: #c8d8e8;
        line-height: 1.8;
        max-width: 620px;
      }

      .hero-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 16px;
      }

      .btn,
      .btn-secondary {
        border: none;
        border-radius: 999px;
        padding: 14px 26px;
        font-weight: 700;
        cursor: pointer;
      }

      .btn {
        background: linear-gradient(135deg, #00ffe1, #1bb7ff);
        color: #05070b;
      }

      .btn-secondary {
        background: rgba(255, 255, 255, 0.05);
        color: #e6f7ff;
        border: 1px solid rgba(255, 255, 255, 0.12);
      }

      .hero-stats {
        display: grid;
        gap: 18px;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        margin-top: 28px;
      }

      .stat-card {
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 24px;
        padding: 20px;
        background: rgba(255, 255, 255, 0.02);
      }

      .stat-card strong {
        display: block;
        font-size: 1.55rem;
        margin-bottom: 8px;
      }

      .hero-visual {
        min-height: 420px;
        border-radius: 32px;
        overflow: hidden;
        background: linear-gradient(180deg, rgba(7, 27, 53, 0.9), rgba(3, 8, 16, 0.96));
        border: 1px solid rgba(255, 255, 255, 0.08);
        position: relative;
      }

      .hero-visual::before {
        content: "";
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at 80% 20%, rgba(0, 255, 255, 0.18), transparent 20%),
          radial-gradient(circle at 30% 90%, rgba(124, 255, 203, 0.14), transparent 18%);
        pointer-events: none;
      }

      .visual-panel {
        position: absolute;
        inset: 0;
        display: grid;
        place-items: center;
        padding: 40px;
      }

      .visual-panel h2 {
        margin: 0;
        font-size: 2rem;
        color: #7cffcb;
        text-transform: uppercase;
        letter-spacing: 0.16em;
      }

      .visual-panel p {
        margin: 18px 0 0;
        max-width: 420px;
        color: #c8d8e8;
        text-align: center;
        line-height: 1.7;
      }

      .products,
      .features {
        margin-top: 56px;
      }

      .section-title {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 24px;
      }

      .section-title small {
        color: #7cffcb;
        letter-spacing: 0.2em;
        text-transform: uppercase;
      }

      .section-title h2 {
        margin: 0;
        font-size: 2.1rem;
      }

      .product-grid {
        display: grid;
        gap: 20px;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      }

      .product-card {
        border-radius: 28px;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.08);
        padding: 24px;
      }

      .product-card h3 {
        margin: 0 0 12px;
        font-size: 1.35rem;
      }

      .product-card p {
        margin: 0 0 18px;
        color: #c8d8e8;
        line-height: 1.75;
      }

      .product-card span {
        display: inline-flex;
        font-weight: 700;
        color: #7cffcb;
      }

      .feature-list {
        display: grid;
        gap: 16px;
      }

      .feature-item {
        display: grid;
        grid-template-columns: 30px 1fr;
        gap: 14px;
        align-items: start;
        padding: 18px 20px;
        border-radius: 20px;
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.06);
      }

      .feature-item div {
        width: 30px;
        height: 30px;
        border-radius: 50%;
        display: grid;
        place-items: center;
        background: rgba(124, 255, 203, 0.12);
        color: #7cffcb;
      }

      footer {
        margin-top: 64px;
        padding-top: 28px;
        border-top: 1px solid rgba(255, 255, 255, 0.08);
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 16px;
        color: #8ca4b3;
        font-size: 0.95rem;
      }

      @media (max-width: 860px) {
        header,
        .hero {
          grid-template-columns: 1fr;
        }

        .hero {
          padding: 48px 0 32px;
        }

        .hero-stats {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>
  <body>
    <div class="page">
      <header>
        <div class="brand">
          <span></span>
          Zona Gaming
        </div>
        <nav>
          <a href="#coleccion">Componentes</a>
          <a href="#ventajas">Ventajas</a>
          <a href="#contacto">Contacto</a>
        </nav>
      </header>

      <section class="hero">
        <div class="hero-copy">
          <small>Tiendá PC Gamer</small>
          <h1>Equiposssss y componentes que elevan tu rendimiento al siguiente nivel.</h1>
          <p>Descubre un catálogo curado de piezas para armado de PC, desde tarjetas gráficas y fuentes hasta teclados mecánicos y enfriamiento líquido. Diseño minimalista, velocidad máxima y estilo gamer real.</p>
          <div class="hero-actions">
            <button class="btn">Ver ofertas</button>
            <button class="btn-secondary">Personalizar PC</button>
          </div>

          <div class="hero-stats">
            <div class="stat-card">
              <strong>+120</strong>
              Modelos optimizados para gaming.
            </div>
            <div class="stat-card">
              <strong>24h</strong>
              Envío rápido y soporte técnico.
            </div>
            <div class="stat-card">
              <strong>5 años</strong>
              Garantía en componentes premium.
            </div>
          </div>
        </div>

        <div class="hero-visual">
          <div class="visual-panel">
            <h2>Lleva tu setup al podio</h2>
            <p>Haz que cada partida sea más inmersiva con hardware confiable, iluminación sutil y un diseño que queda perfecto en tu escritorio.</p>
          </div>
        </div>
      </section>

      <section id="coleccion" class="products">
        <div class="section-title">
          <small>Top ventas</small>
          <h2>Selección de componentes</h2>
        </div>
        <div class="product-grid">
          <article class="product-card">
            <h3>GPU Titan X</h3>
            <p>Rendimiento extremo para juegos AAA, streaming y renderizado sin compromisos.</p>
            <span>$1,299 USD</span>
          </article>

          <article class="product-card">
            <h3>Placa madre NeoCore</h3>
            <p>Compatibilidad total con DDR5, PCIe 5.0 y overclock estable para tus builds más audaces.</p>
            <span>$249 USD</span>
          </article>

          <article class="product-card">
            <h3>Fuente HyperFlow 850W</h3>
            <p>Eficiencia Gold con cableado modular y protección inteligente para cada partida.</p>
            <span>$159 USD</span>
          </article>
        </div>
      </section>

      <section id="ventajas" class="features">
        <div class="section-title">
          <small>Por qué elegirnos</small>
          <h2>Ventajas del setup gamer</h2>
        </div>
        <div class="feature-list">
          <div class="feature-item">
            <div>1</div>
            <div>
              <strong>Diseño minimalista</strong>
              <p>Una estética limpia y futurista pensada para que tu pc destaque sin recargar tu escritorio.</p>
            </div>
          </div>
          <div class="feature-item">
            <div>2</div>
            <div>
              <strong>Calidad premium</strong>
              <p>Seleccionamos componentes de marcas reconocidas con rendimiento estable y duradero.</p>
            </div>
          </div>
          <div class="feature-item">
            <div>3</div>
            <div>
              <strong>Asesoría experta</strong>
              <p>Nuestro equipo te ayuda a elegir las piezas ideales según tu presupuesto y estilo de juego.</p>
            </div>
          </div>
        </div>
      </section>

      <footer id="contacto">
        <div>Zona Gaming · Venta de componentes PC · Minimalismo gamer</div>
        <div>¿Listo para mejorar tu setup? Contacto: hola@zonagaming.com</div>
		xdsdsd
      </footer>
    </div>
  </body>
</html>
